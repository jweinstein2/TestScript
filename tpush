#!/bin/bash
myPath=$(eval "pwd")
scriptPath=$(dirname $(readlink -f $0))
message=$1

echo " fucker $message"

cd $scriptPath
git add *
git commit -m "fucker $message"
git push origin master
cd $myPath
